#include<stdio.h>

void newton();

int main() {
	
/* Newton algorithm:

define - f,df,H,x,iter
while (iter < maxiter)
	dx = lineq_solve(df(x),H(x))
	if (abs(df(x)+H(x)*dx)<eps)
		return x
	end

	s = dx
	while (f(x+s) >= f(x) + a*s'*df(x))
		s = s/2
	end

	x = x + s
	iter = iter + 1
end

*/
	
	
	
}
